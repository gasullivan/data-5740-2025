# Feedback 21/20

When you're doing data exploration, another handy function to use on each column is something like `cyber_df[col].value_counts().sort_values(ascending=False)[:10]`. The `value_counts()` method provides a bit more detail than just the list of value and `sort_values()` will give you the most frequently occuring values.

In our class discussions about this, I really appreciate how you brought real-world knowledge and experience in to improve the conversation and provide context for things like how phishing click rates might really be measured and if they could be > 100%.  (+1)

Very nice summary in the conclusion.
